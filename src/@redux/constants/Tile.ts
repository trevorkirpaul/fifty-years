const NAME: string = "TILE";

/**
 * This action will start the game
 * and add the starting empty tiles
 * to the tile reducer
 */
const CREATE_GAME: string = `@@${NAME}/CREATE_GAME`;

export { NAME, CREATE_GAME };
