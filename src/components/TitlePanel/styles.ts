import styled from "styled-components";

const Wrapper = styled.div``;

const Title = styled.h1``;

const SubTitle = styled.h2``;

const Text = styled.p``;

export { Wrapper, Title, SubTitle, Text };
